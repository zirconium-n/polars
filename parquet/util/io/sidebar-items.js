initSidebarItems({"struct":[["FileSink","Struct that represents `File` output stream with position tracking. Used as a sink in file writer."],["FileSource","Struct that represents a slice of a file data with independent start position and length. Internally clones provided file handle, wraps with a custom implementation of BufReader that resets position before any read."]],"trait":[["ParquetReader","ParquetReader is the interface which needs to be fulfilled to be able to parse a parquet source."],["Position","Position trait returns the current position in the stream. Should be viewed as a lighter version of `Seek` that does not allow seek operations, and does not require mutable reference for the current position."],["TryClone","TryClone tries to clone the type and should maintain the `Seek` position of the given instance."]]});